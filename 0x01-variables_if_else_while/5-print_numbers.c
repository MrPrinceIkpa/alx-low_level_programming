#include <stdio.h>
/**
 *main - prints out single digits in base 10
 *Return: returns 0 on success
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
